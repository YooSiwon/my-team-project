<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.MemberMapper">

<select id="getList" resultType="org.zerock.domain.MemberVO">
<![CDATA[
select * from Member
]]>
</select>

	<insert id="insert">
		insert into Member (id, password, name, nickname, phone, email,)
		values (seq_board.nextval, 
				#{id}, 
				#{password}, 
				#{name}, 
				#{nickname}, 
				#{phone}, 
				#{email},)
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="Member" order="BEFORE" resultType="long">
		select seq_board.nextval from dual
		</selectKey>
	
		insert into Member (Member, id, password, name, nickname, phone, email,)
		values (#{id}, 
				#{password}, 
				#{name}, 
				#{nickname}, 
				#{phone}, 
				#{email},)
	</insert>
	
	<select id="read" resultType="org.zerock.domain.BoardVO">
		select * from Member 
	</select>
	
	<delete id="delete">
		delete from Member
	</delete>
	
	<update id="update">
	UPDATE Member
	SET
	  id = #{id},
	  password = #{password},
	  name = #{name},
	  nickname = #{nickname},
	  phone = #{phone},
	  email = #{email},
	WHERE
	  bno = #{bno}
	</update>

</mapper>

 
				









